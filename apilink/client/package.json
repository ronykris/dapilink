{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "tsc -p . && ts-node src/test.ts",
    "build": "tsc -p . && ts-node src/ipfs.ts",
    "start": "tsc -p . && ts-node src/index.ts",
    "testClient": "tsc -p . && ts-node src/testLinkClient.ts",
    "ipfs": "tsc -p . && ts-node src/ipfs.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@helia/json": "^1.0.2",
    "@oasisprotocol/sapphire-paratime": "^1.1.1",
    "child_process": "^1.0.2",
    "dotenv": "^16.3.1",
    "ethers": "^5.4",
    "helia": "^2.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
