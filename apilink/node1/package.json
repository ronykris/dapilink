{
  "name": "node",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p .",
    "login": "tsc -p . && ts-node src/login.ts",
    "start": "tsc -p . && ts-node src/index.ts",
    "decode": "tsc -p . && ts-node src/decode.ts",
    "listen": "tsc -p . && ts-node src/eventListener.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "child_process": "^1.0.2",
    "dotenv": "^16.3.1",
    "ethers": "^5.4",
    "serpapi": "^2.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "web3": "^4.1.1"
  }
}
